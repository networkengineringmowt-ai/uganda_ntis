name: Backup on Push
on:
  push:
    branches: [main, gh-pages]
jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Log backup
        run: echo "Backup triggered at $(date) for ${{ github.repository }} commit ${{ github.sha }}"
