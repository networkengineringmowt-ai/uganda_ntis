PORT=5000
DB_USER=postgres
DB_HOST=localhost
DB_NAME=uganda_enterprise_db
DB_PASSWORD=your_super_secret_password_here
DB_PORT=5432
# Comma-separated list of frontend origins allowed to call this API.
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://networkengineringmowt-ai.github.io
# Force SSL on/off for the Postgres connection. Leave unset to auto-detect
# (on whenever DB_HOST isn't localhost -- e.g. Supabase, RDS, Azure PG).
# DB_SSL=true
